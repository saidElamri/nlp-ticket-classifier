apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: nlp-model-cache-pvc
  labels:
    app: nlp-pipeline
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
